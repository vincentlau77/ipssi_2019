#!/bin/bash

j=0
while read n; do
	                sum=$(($sum+$n))
			                j=$((j+1))

					        done
						        mean=$((sum/j))
							echo $mean
							echo $sum
							        exit
